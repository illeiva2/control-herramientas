# datos reales: NUNCA subir (contiene nombres y DNI del personal)
data/

# builds y paquetes
dist/
build/
instalador/
*.zip
*.spec

# python
__pycache__/
.venv/

# secretos: credenciales de Supabase / conexión (NUNCA subir)
.env
.env.code

# bases de datos y respaldos con datos del personal (NUNCA subir)
panol.db
respaldo-*.json
