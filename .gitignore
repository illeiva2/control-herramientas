# datos reales: NUNCA subir (contiene nombres y DNI del personal)
data/

# builds y paquetes
dist/
build/
instalador/
*.zip
*.spec

# python
__pycache__/
.venv/

# secretos: credenciales de conexión (NUNCA subir)
.env
.env.code
.env.servidor
setup_db.sql

# bases de datos y respaldos con datos del personal (NUNCA subir)
panol.db
respaldo-*.json
